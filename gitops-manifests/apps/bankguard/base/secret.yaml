# ATTENTION: CE FICHIER EST UN TEMPLATE
# NE JAMAIS COMMITTER DE VRAIS SECRETS ICI
# Les secrets réels sont injectés via:
# 1. Kustomize secretGenerator (dans les overlays)
# 2. ExternalSecrets operator
# 3. HashiCorp Vault

apiVersion: v1
kind: Secret
metadata:
  name: bankguard-secrets
type: Opaque
stringData:
  # TEMPLATES SEULEMENT
  DB_PASSWORD: "REPLACE_WITH_REAL_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_REDIS_PASSWORD"
  
  # Pour le futur
  JWT_SECRET_KEY: "REPLACE_WITH_JWT_SECRET"
  ENCRYPTION_KEY: "REPLACE_WITH_ENCRYPTION_KEY"
