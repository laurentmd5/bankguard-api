# gitops-manifests/kustomization.yaml
# POINT D'ENTRÉE PRINCIPAL pour Argo CD
# Référence TOUS les composants à déployer

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # 1. Namespace (géré par GitOps)
  - namespaces/bankguard.yaml
  
  # 2. Application BankGuard (environnement dev)
  - apps/bankguard/overlays/dev/
  
  # Note: Les bases de données (PostgreSQL/Redis) seront ajoutées
  # dans un second temps pour garder la complexité gérable
  # - apps/databases/overlays/dev/
  
  # Note: Monitoring sera ajouté en Phase 5
  # - apps/monitoring/
